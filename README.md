# 🚉 ИСИД

Система компьютерного зрения для автоматического мониторинга работников и подвижного состава в депо РЖД.  
Позволяет определять **поезда**, **людей**, **номера локомотивов**, **зоны безопасности**, **инциденты** и **активность персонала**.

---

## ⚙️ Технологический стек

**Компьютерное зрение:**
- Детекция объектов — `YOLOv11n (Ultralytics)`
- Трекинг объектов — встроенный `Centroid Tracker`
- OCR номеров — `EasyOCR`
- Анализ поз — `YOLO Pose` *(опционально)*

**Инфраструктура и исполнение:**
- Язык: `Python 3.10+`
- Запуск через `launch.bat` — единое меню для установки зависимостей, рисования зон и запуска системы
- Локальное окружение: `venv/`  
- Модули проекта — в папке `python/`

**Хранение данных и аналитика:**
- Логи: JSON-файлы в `logs/`
- Графики: автоматически строятся после каждой сессии (по количеству работников и инцидентов)
- Возможна интеграция с `PostgreSQL` или `TimescaleDB` для долговременного хранения

---

## 📁 Структура проекта
ИСИД/
│ launch.bat
│ requirements.txt
│ main.py
│ draw_zones.py
│
├─python/
│ ├─ (основные библиотеки и скрипты)
│
├─models/
│ ├─ yolov11n.pt
│ └─ yolo11n-pose.pt
│
├─video/
│ └─ remonty.mov
│
├─zones/
│ ├─ yellow_zone.json
│ └─ green_zone.json
│
├─logs/
│ └─ session_*.json
│
└─venv/

Запусти launch.bat:
- `1` — для автоматической установки зависимостей (создаст venv и установит пакеты)
- `2` — для запуска `draw_zones.py` и рисования жёлтой/зелёной зон
- `3` — для старта системы (`main.py`)
- `4` — выход

---

### 🔹 2. Установка зависимостей вручную (альтернатива)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
